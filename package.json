{
  "name": "amqp-pro",
  "version": "0.1.12",
  "description": "A high-level amqp client with built-in reconnect",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "hallsamuel90",
  "license": "MIT",
  "homepage": "https://github.com/hallsamuel90/amqp-pro#readme",
  "keywords": [
    "amqp",
    "rabbitmq",
    "queue"
  ],
  "dependencies": {
    "amqplib": "^0.6.0"
  },
  "devDependencies": {
    "@types/amqplib": "^0.5.17",
    "@types/jest": "^26.0.20",
    "eslint": "^7.21.0",
    "jest": "^26.6.3",
    "jest-silent-reporter": "^0.4.0",
    "ts-jest": "^26.5.2",
    "typescript": "^4.2.2"
  },
  "scripts": {
    "test": "./bin/test-runner.sh",
    "test:unit": "./bin/test-runner.unit.sh",
    "build": "tsc",
    "lint": "eslint",
    "ci": "yarn lint && yarn test && yarn build"
  }
}
